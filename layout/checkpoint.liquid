<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ page_title }}</title>
  {{ content_for_header }}
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      background: #f9fafb;
    }
    .checkpoint-container {
      background: white;
      padding: 40px;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      max-width: 500px;
      width: 100%;
      text-align: center;
    }
    h1 {
      font-size: 24px;
      margin-bottom: 20px;
      color: #202223;
    }
    .checkpoint-form {
      margin-top: 30px;
    }
    .h-captcha {
      margin: 30px auto;
      display: inline-block;
      z-index: 9999;
    }
    button {
      background: #000;
      color: white;
      border: none;
      padding: 12px 24px;
      border-radius: 4px;
      font-size: 16px;
      cursor: pointer;
      margin-top: 20px;
    }
    button:hover {
      background: #333;
    }
  </style>
</head>
<body>
  <div class="checkpoint-container">
    <h1>{{ 'general.checkpoint.title' | t }}</h1>
    <p>{{ 'general.checkpoint.subtitle' | t }}</p>
    
    {{ content_for_layout }}
  </div>
</body>
</html>
